# medipic handoff

Last updated: 2026-05-01, Asia/Seoul

## Current goal

Medipic landing is being redesigned toward a premium Japanese-style telehealth storefront inspired by the interface rhythm of forhers.com:

- Full-screen category sections
- Image-led backgrounds
- Product/card rail connected to intake
- Mouse hover light/flash motion
- Scroll-friendly vertical composition
- No fabricated product names, prices, medical effects, reviews, or doctor profiles

## Current local repo

Workspace:

```text
C:\Users\info\OneDrive\Documents\New project\codex-medipic-app
```

Current local URL:

```text
http://localhost:3002/
```

Run command:

```bash
npm.cmd run dev -- --port 3002
```

## Current git state

There are uncommitted working-tree changes.

Modified files:

- `src/app/page.tsx`
- `src/app/layout.tsx`
- `src/app/globals.css`

Untracked files:

- `public/images/medipic/`
- `medipic-dev-3002.log`
- `medipic-dev-3002-live.log`
- `medipic-dev-3003.log`

Do not delete the log files without user confirmation. They are not needed for the app, but local deletion is still a data-removal action.

## Verified status

Latest verification completed before this handoff:

```bash
npm.cmd run lint
npm.cmd run build
```

Result:

- `lint`: passed with existing 11 warnings
- `build`: passed
- Next.js warning still present: `middleware` file convention is deprecated and should eventually migrate to `proxy`

## Active landing implementation

Main file:

```text
src/app/page.tsx
```

Current behavior:

- Root page `/` is a storefront concept page.
- Hero has category navigation and Medipic CTA.
- Four sections are stacked vertically:
  - `weight`
  - `hair`
  - `women`
  - `skin`
- Every product-style card links to a matching intake route:
  - `/intake/weight`
  - `/intake/hair`
  - `/intake/menopause`
  - `/intake/skin`
- `MotionStage` applies background image, pointer spotlight, and hover flash.
- `ProductMock` is still CSS mock product art. It can later be replaced by actual product photos.
- Product cards are currently placeholders and should not imply actual efficacy or availability.

Important issue:

- Some `ja` and `ko` copy in `src/app/page.tsx` is mojibake from earlier encoding issues.
- The app still builds because the strings are syntactically valid.
- Before real copy review, replace `ja` and `ko` copy with clean text or temporarily force English/Korean-only copy.
- `src/hooks/useLocale.ts` reload persistence currently only restores `en` or `ja`, not `ko`. If Korean needs stable reload persistence, update it to include `ko`.

## Generated images already saved in project

Current concept images:

```text
public/images/medipic/category-moodboard.png
public/images/medipic/weight-care.png
public/images/medipic/hair-care.png
public/images/medipic/health-review.png
public/images/medipic/skincare.png
```

These are AI-generated concept assets. They are safe as layout placeholders, but final product/legal/medical assets should replace them later.

## Interrupted image work

The user requested:

1. Backgrounds should use people to feel more trustworthy.
2. Product/background images were too cropped.
3. Sections can be taller vertically.
4. Then the user said: make it feel more Japanese.
5. Then the user changed direction: do not modify the page yet; show a 2x2 numbered sample for confirmation.

Before interruption, four new people-centered images were generated by the image tool but were not yet copied into `public/images/medipic`.

Default generated image directory:

```text
C:\Users\info\.codex\generated_images\019ddd4f-04d8-7192-8d01-217976403d9c
```

Most recent generated images in that directory are likely the people-centered backgrounds:

- weight care: adult in calm home-health phone consultation, deep olive tone
- hair support: adult in bathroom/vanity hair-care context, blue-aqua tone
- women's health: adult woman with phone/tablet in calm teal interior
- medical skincare: adult in premium vanity/skincare context, rose/plum tone

Next session should inspect those recent files visually before copying them into the project.

## Latest user instruction to honor next

The newest design instruction before saving was:

```text
페이지 수정없이 샘플로 2*2 그리드 형태로 숫자 넣어서 컨펌받아봐
```

Meaning:

- Do not change the current landing page yet.
- Create a separate confirmation sample first.
- The sample should be a 2x2 grid.
- Number each option so the user can choose.
- Direction should feel more Japanese than the current design.
- Use people-centered trustworthy backgrounds rather than cropped product-heavy backgrounds.

Recommended next action:

1. Create a separate preview file or route, not modifying `/`.
2. Good option: create `src/app/design-sample/page.tsx`.
3. Show a 2x2 grid with numbered concepts:
   - `01 Weight care`
   - `02 Hair support`
   - `03 Women's health`
   - `04 Medical skincare`
4. Use the recent people-centered generated images after inspecting/copying them.
5. Make the cards taller, Japanese editorial, calmer, and less product-cropped.
6. Ask the user to choose a number or direction before touching the main landing.

Potential sample URL:

```text
http://localhost:3002/design-sample
```

## Design direction for the next sample

Japanese-style direction should be:

- More quiet editorial, less aggressive ecommerce
- High trust, clinic-adjacent, but not hospital-looking
- More negative space
- Softer typography density
- Muted greens, ivory, charcoal, soft aqua, muted rose
- People and lifestyle context first
- Product objects small and secondary
- Avoid fake medical claims and fake testimonials

## Commands for home PC continuation

```bash
cd "C:\Users\info\OneDrive\Documents\New project\codex-medipic-app"
npm.cmd install
npm.cmd run dev -- --port 3002
```

Verification:

```bash
npm.cmd run lint
npm.cmd run build
```

Expected result:

- `lint` passes with 11 existing warnings
- `build` passes

## Notes

- `localhost:3001` may point to a different local repo (`C:\codex\codex-medipic-app`) on this machine. Use `3002` for this OneDrive workspace.
- In-app browser screenshot capture previously timed out on animated/image-heavy pages, but DOM checks, HTTP response, lint, and build were normal.
- Do not add real product names, prices, efficacy claims, reviews, or doctor profiles until the user provides approved content.
